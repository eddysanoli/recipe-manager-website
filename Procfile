# Command to execute in order to start the application
#   - web: Processed type. Declares that this process type will receive web process
#   - gunicorn django_project.wsgi: File that gunicorn will try to use to open the web page.
#     Generally the file should have the same name as the project folder in your django files.
#   - wsgi: Web Server Gateway Interface 
web: gunicorn recipe_manager.wsgi